name: Test

on:
  push:
  pull_request:

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - run: echo "$DATA"
        env:
          DATA: ${{ toJson(inputs) }}
      - run: echo "$DATA"
        env:
          DATA: ${{ inputs.testing }}
      - run: echo "$DATA"
        env:
          DATA: ${{ inputs.testing || 'default' }}
